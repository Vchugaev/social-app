# GitHub Actions Workflows

## 📊 Схема CI/CD

```
┌─────────────────────────────────────────────────────────────┐
│                     Developer Push                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    GitHub Actions                           │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │   Lint   │  │   Test   │  │  Build   │                 │
│  │          │  │          │  │          │                 │
│  │ ESLint   │  │ Jest     │  │ NestJS   │                 │
│  │ Prettier │  │ Coverage │  │ Prisma   │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│       │             │              │                        │
│       └─────────────┴──────────────┘                        │
│                     │                                       │
│                     ▼                                       │
│              ┌──────────────┐                              │
│              │   Success?   │                              │
│              └──────────────┘                              │
│                     │                                       │
└─────────────────────┼───────────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
        ▼                           ▼
┌───────────────┐          ┌────────────────┐
│   develop     │          │      main      │
│   branch      │          │     branch     │
└───────────────┘          └────────────────┘
        │                           │
        ▼                           ▼
┌───────────────┐          ┌────────────────┐
│   Staging     │          │   Production   │
│   Deploy      │          │     Deploy     │
│               │          │  (with review) │
└───────────────┘          └────────────────┘
```

## 🔄 Workflows

### 1. ci-cd.yml

**Триггеры:**
- Push в `main` или `develop`
- Pull Request в `main` или `develop`
- Изменения в `back/**`

**Jobs:**

```yaml
lint → test → build → deploy-staging (develop)
                   → deploy-production (main)
```

**Время выполнения:** ~5-10 минут

### 2. docker.yml

**Триггеры:**
- Push в `main` или `develop`
- Теги `v*`

**Jobs:**

```yaml
build-and-push → GitHub Container Registry
```

**Время выполнения:** ~3-5 минут

### 3. release.yml

**Триггеры:**
- Теги `v*`

**Jobs:**

```yaml
generate-changelog → create-release
```

**Время выполнения:** ~1 минута

## 🎯 Использование

### Автоматический деплой

```bash
# Staging
git push origin develop

# Production
git push origin main
```

### Создание релиза

```bash
git tag v1.0.0
git push origin v1.0.0
```

### Pull Request

```bash
git checkout -b feature/new-feature
# ... changes ...
git push origin feature/new-feature
# Create PR on GitHub
```

## 📝 Статусы

- ✅ **Success** - Все проверки прошли
- ❌ **Failed** - Есть ошибки
- 🟡 **Pending** - Выполняется
- ⏸️ **Waiting** - Ожидает подтверждения

## 🔐 Secrets

Необходимые секреты в Settings → Secrets:

```
STAGING_HOST
STAGING_USER
STAGING_SSH_KEY

PRODUCTION_HOST
PRODUCTION_USER
PRODUCTION_SSH_KEY
```

## 🌍 Environments

- **staging** - Автоматический деплой из `develop`
- **production** - Деплой из `main` с подтверждением

## 📊 Badges

Добавьте в README.md:

```markdown
![CI/CD](https://github.com/username/repo/workflows/Backend%20CI/CD/badge.svg)
![Docker](https://github.com/username/repo/workflows/Docker%20Build%20&%20Push/badge.svg)
```
